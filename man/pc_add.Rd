% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_compare.R
\name{pc_add}
\alias{pc_add}
\title{Add a ggplot2 Layer to a \code{pc_interactive} Object}
\usage{
pc_add(x, ...)
}
\arguments{
\item{x}{A \code{pc_interactive} object returned by
\code{plot_compare(..., interactive = TRUE)}.}

\item{...}{One or more ggplot2 layer objects (e.g. \code{\link[ggplot2:labs]{ggplot2::ggtitle()}},
\code{\link[ggplot2:theme]{ggplot2::theme()}}, \code{\link[ggplot2:scale_manual]{ggplot2::scale_colour_manual()}}).}
}
\value{
The updated \code{pc_interactive} object (invisibly), with layers queued
for application at print / \code{\link[=as_plotly]{as_plotly()}} time.
}
\description{
Queues a ggplot2 layer (theme, scale, title, etc.) to be applied to the
internal ggplot before it is converted to a plotly widget.

Use \code{pc_add()} instead of \code{+} when working with ggplot2 >= 4.0.0, because
ggplot2's S7-based \code{+} generic intercepts the call before S3 dispatch can
reach \code{+.pc_interactive}.
}
\examples{
\dontrun{
p <- plot_compare(res, interactive = TRUE)
p <- pc_add(p, ggplot2::ggtitle("My title"), ggplot2::theme_minimal())
print(p)   # converts to plotly with title and theme applied
}

}
\seealso{
\code{\link[=plot_compare]{plot_compare()}}, \code{\link[=as_plotly]{as_plotly()}}
}
