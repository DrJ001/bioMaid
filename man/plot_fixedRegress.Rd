% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_fixedRegress.R
\name{plot_fixedRegress}
\alias{plot_fixedRegress}
\title{Plot Output from fixedRegress()}
\usage{
plot_fixedRegress(
  res,
  type = c("regress", "quadrant"),
  treatments = NULL,
  highlight = "default",
  theme = ggplot2::theme_bw(),
  return_data = FALSE,
  ...
)
}
\arguments{
\item{res}{A list returned by \code{\link[=fixedRegress]{fixedRegress()}}.}

\item{type}{Character string selecting the plot type. One of
\code{"regress"} (default) or \code{"quadrant"}.}

\item{treatments}{Character vector restricting which conditioned treatments
are included.  \code{NULL} (default) includes all conditioned treatments.}

\item{highlight}{Controls genotype annotation. One of:
\describe{
\item{\code{"default"}}{Automatically selects 6 genotypes using the
polar-angle algorithm (3 top-right, 3 bottom-left).}
\item{character vector}{Highlight exactly the named genotypes.}
\item{\code{NULL}}{No highlighting.}
}
Default is \code{"default"}.}

\item{theme}{A complete ggplot2 theme object. Default
\code{\link[ggplot2:ggtheme]{ggplot2::theme_bw()}}.}

\item{return_data}{Logical. If \code{TRUE} returns the tidy data frame rather
than the plot. Default \code{FALSE}.}

\item{...}{Additional arguments passed to the background
\code{geom_point()} call (e.g. \code{size}, \code{alpha}).}
}
\value{
A \code{ggplot} object (when \code{return_data = FALSE}) or a \code{data.frame}
(when \code{return_data = TRUE}).
}
\description{
Produces one of two ggplot2 visualisations from a \code{\link[=fixedRegress]{fixedRegress()}} result
object, analogous to \code{\link[=plot_randomRegress]{plot_randomRegress()}} for fixed-effects (BLUE) output.
Both plot types return a \code{ggplot} object that can be further customised
with the standard \code{+} operator.
}
\details{
The two \code{type} options are:
\describe{
\item{\code{"regress"}}{Grid of scatter plots faceted by BLUP pair (rows) and
group (columns).  Each panel plots the raw conditioned-treatment BLUEs
(y) against the conditioning-treatment BLUEs (x) for one group × one
treatment pair.  A dotted OLS regression line is drawn and the slope
\eqn{\hat{\beta}} is annotated in the top-left corner of each panel.
Unlike the random-effects analogue the line need not pass through the
origin because OLS includes an intercept.}
\item{\code{"quadrant"}}{Grid of scatter plots faceted by BLUP pair (rows) and
group (columns).  Each panel plots the response index (OLS residual,
y) against the unconditional treatment BLUE (efficiency, x).  Dotted
zero reference lines divide each panel into four quadrants.}
}

\strong{Variety highlighting} — see \code{\link[=plot_randomRegress]{plot_randomRegress()}} for the full
description of the polar-angle algorithm used to select the default six
genotypes.  The same three archetypes (efficiency, balanced,
responsiveness) are chosen from the top-right and bottom-left quadrants of
the efficiency × response-index space, averaged across all groups.

The output data frames returned when \code{return_data = TRUE} use standardised
column names \code{"Group"} (the grouping variable) and \code{"Genotype"} (the
regression unit) regardless of the original column names in
\code{res$blues}.
}
\examples{
\dontrun{
res <- fixedRegress(model, term = "Treatment:Site:Genotype",
                    by = "Site", levs = c("T0", "T1", "T2"))

# Regression plot with default 6 highlighted genotypes
plot_fixedRegress(res)

# Quadrant plot
plot_fixedRegress(res, type = "quadrant")

# User-specified highlights
plot_fixedRegress(res, type = "quadrant",
                  highlight = c("Gen01", "Gen15"))

# Suppress highlighting
plot_fixedRegress(res, type = "regress", highlight = NULL)

# Retrieve tidy data frame
df <- plot_fixedRegress(res, type = "quadrant", return_data = TRUE)
}

}
\seealso{
\code{\link[=fixedRegress]{fixedRegress()}}, \code{\link[=plot_randomRegress]{plot_randomRegress()}}, \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}
}
