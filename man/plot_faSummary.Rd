% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_faSummary.R
\name{plot_faSummary}
\alias{plot_faSummary}
\title{Visualise a factor analytic model summary}
\usage{
plot_faSummary(
  res,
  type = c("VAF", "heatmap", "loadings", "regress", "added"),
  term = NULL,
  order = c("loading", "asis", "cluster"),
  varieties = "default",
  n_varieties = 6L,
  tol = 0.85,
  theme = ggplot2::theme_bw(),
  return_data = FALSE,
  ...
)
}
\arguments{
\item{res}{An object of class \code{"faSummary"} from \code{\link[=faSummary]{faSummary()}}.}

\item{type}{Character; the visualisation to produce.  One of
\code{"VAF"} (default), \code{"heatmap"}, \code{"loadings"}, \code{"regress"}, \code{"added"}.
May be abbreviated.}

\item{term}{Character; which FA term of \code{res} to plot.  The default,
\code{NULL}, uses the first term that carries loadings.}

\item{order}{Environment ordering for \code{"VAF"} and \code{"heatmap"}.  One of
\code{"loading"} (default; by first-factor loading, descending), \code{"asis"} (the
order of the factor levels) or \code{"cluster"} (average-linkage hierarchical
clustering of \eqn{1 - C}).}

\item{varieties}{Varieties to display in \code{"regress"} and \code{"added"}.
Either \code{"default"} (the highest- and lowest-scoring variety on each
factor) or a character vector of variety names.}

\item{n_varieties}{Positive integer; maximum number of varieties to select
when \code{varieties = "default"}.  Default \code{6L}.}

\item{tol}{Numeric in [0, 1]; radius threshold used to flag
well-explained environments in the \code{"loadings"} plot.  Default \code{0.85}.}

\item{theme}{A complete ggplot2 theme object.  Default
\code{\link[ggplot2:ggtheme]{ggplot2::theme_bw()}}.}

\item{return_data}{Logical; if \code{TRUE}, return a list with elements \code{plot}
and \code{data}.  Default \code{FALSE}.}

\item{...}{Reserved for future use; currently ignored.}
}
\value{
When \code{return_data = FALSE} (default) a \code{ggplot} object; otherwise a
named list with elements \code{plot} and \code{data}.  For \code{type = "VAF"} the \code{data}
element is itself a list with components \code{env} and \code{summary}.
}
\description{
Diagnostic plots for the factor analytic structure returned by
\code{\link[=faSummary]{faSummary()}}: variance accounted for, the genetic correlation heatmap,
correlation-scaled loadings, and the regression of genotype BLUPs on
environment loadings.
}
\section{Plot types}{
\describe{
\item{\code{"VAF"}}{Stacked 100\% bar chart of the Variance Accounted For in
each environment: one segment per FA factor (sequential blues, from the
bottom) plus the specific variance (grey, on top).  The dashed line
marks the overall mean proportion explained.}
\item{\code{"heatmap"}}{Genetic correlation between environments, on a
diverging palette centred at zero.  Use \code{order} to reveal block
structure.}
\item{\code{"loadings"}}{Correlation-scaled loadings plotted as vectors from
the origin, one panel per pair of factors, with the unit circle for
reference.  Environments whose vector reaches beyond \code{tol} are well
explained by that pair of factors and are drawn solid; the rest are
dotted.  Requires \eqn{k \ge 2}.}
\item{\code{"regress"}}{For each selected variety and factor, the genotype
BLUPs plotted against the environment loadings, with a line whose slope
is that variety's factor score.}
\item{\code{"added"}}{As \code{"regress"}, but the BLUPs are first adjusted by
removing the fitted contribution of every other factor, so the plotted
slope aligns with the scatter.  This is the correct display when
\eqn{k \ge 2}.}
}
}

\examples{
\dontrun{
fas <- faSummary(model)

plot_faSummary(fas, type = "VAF")
plot_faSummary(fas, type = "heatmap", order = "cluster")
plot_faSummary(fas, type = "loadings")
plot_faSummary(fas, type = "added", varieties = c("Var01", "Var22"))

out <- plot_faSummary(fas, type = "VAF", return_data = TRUE)
head(out$data$env)
}

}
\seealso{
\code{\link[=faSummary]{faSummary()}}, \code{\link[=fastIC]{fastIC()}}, \code{\link[=plot_fastIC]{plot_fastIC()}}
}
