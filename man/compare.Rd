% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare}
\alias{compare}
\title{Multiple Comparison Criteria for ASReml-R Predicted Values}
\usage{
compare(
  model,
  term,
  by = NULL,
  type = c("HSD", "LSD", "Bonferroni"),
  pev = TRUE,
  alpha = 0.05,
  ...
)
}
\arguments{
\item{model}{An ASReml-R V4 model object.}

\item{term}{Character string specifying the \code{classify} term, e.g.
\code{"Treatment:Genotype"}.  Predictions are obtained via
\code{\link[asreml:predict.asreml]{asreml::predict.asreml()}}.}

\item{by}{Grouping specification.  Comparisons are performed
independently within each level of the group.  Accepted forms:
\describe{
\item{\code{NULL} (default)}{All predictions form one group.}
\item{Single string \code{"Site"}}{Split by the \code{Site} factor in \code{term}.}
\item{Colon string \code{"Site:Treatment"}}{Combine \code{Site} and \code{Treatment}
into a single grouping variable and split by their interaction.}
\item{Character vector \code{c("Site", "Treatment")}}{Equivalent to the
colon form above.}
}
All \code{by} variables must be present in \code{term}.  At least one variable in
\code{term} must remain for comparisons to be made.}

\item{type}{Comparison type.  One of \code{"HSD"} (Tukey, default),
\code{"LSD"} (unadjusted), or \code{"Bonferroni"}.}

\item{pev}{Logical.  If \code{TRUE} (default) the prediction error
variance (PEV) from \code{predict()$vcov} is used.  If \code{FALSE} and \code{term}
is in the random part of the model, the posterior variance
\eqn{\bm{G} - \text{PEV}} is used instead.}

\item{alpha}{Significance level.  Default \code{0.05}.}

\item{...}{Additional arguments forwarded to
\code{\link[asreml:predict.asreml]{asreml::predict.asreml()}}.}
}
\value{
A data frame with one row per non-missing predicted value,
containing:
\describe{
\item{Factor columns}{All factor variables from \code{term}.}
\item{\code{predicted.value}}{The BLUE or BLUP.}
\item{\code{std.error}}{Prediction standard error.}
\item{\code{{type}}}{The comparison criterion (constant within each group).
Two predicted values in the same group are significantly different if
their absolute difference exceeds this value.}
\item{\code{avsed}}{The average SED for the group (constant within group).}
}
}
\description{
Computes a pairwise comparison criterion (HSD, LSD, or Bonferroni-corrected
LSD) for predicted values obtained from an ASReml-R model, optionally
within groups defined by a subset of the factors in \code{term}.

For each group the function computes the \strong{average SED} across all
\eqn{\binom{n_g}{2}} pairs of predictions:

\deqn{
  \overline{\text{SED}} = \frac{1}{\binom{n_g}{2}}
  \sum_{i < j} \sqrt{V_{ii} + V_{jj} - 2V_{ij}}
}

where \eqn{V_{ij}} is the \eqn{(i,j)} element of the prediction error
variance-covariance matrix.  The comparison criterion is then:

\describe{
\item{\code{"HSD"} (Tukey)}{
\eqn{\displaystyle\frac{\overline{\text{SED}}}{\sqrt{2}} \times
    q_{1-\alpha}(n_g,\, \nu)}, where \eqn{q} is the Studentised range
quantile with \eqn{n_g} means and \eqn{\nu = \texttt{model\$nedf}}
error df.  Two means are declared significantly different if their
absolute difference exceeds the HSD.}
\item{\code{"LSD"} (unadjusted)}{
\eqn{\overline{\text{SED}} \times t_{\alpha/2,\,\nu}}.
No multiplicity correction; liberal for large numbers of comparisons.}
\item{\code{"Bonferroni"}}{
\eqn{\overline{\text{SED}} \times
    t_{\alpha/(2m),\,\nu}}, where \eqn{m = \binom{n_g}{2}} is the
number of pairs.  Controls the family-wise error rate.}
}
}
\examples{
\dontrun{
## HSD for Treatment:Genotype, one group per Site
res <- compare(model,
               term = "Treatment:Site:Genotype",
               by   = "Site",
               type = "HSD")

## LSD computed within each Treatment x Site combination
res2 <- compare(model,
                term = "Treatment:Site:Genotype",
                by   = c("Treatment", "Site"),
                type = "LSD")

## Equivalently using colon notation
res3 <- compare(model,
                term = "Treatment:Site:Genotype",
                by   = "Treatment:Site",
                type = "LSD")

## Bonferroni-corrected LSD, single global group
res4 <- compare(model,
                term  = "Treatment:Genotype",
                type  = "Bonferroni",
                alpha = 0.05)

## Posterior variance (BLUPs)
res5 <- compare(model,
                term = "Treatment:Genotype",
                by   = "Treatment",
                pev  = FALSE)
}

}
\seealso{
\code{\link[=waldTest]{waldTest()}} for pairwise p-values, \code{\link[asreml:predict.asreml]{asreml::predict.asreml()}}
}
