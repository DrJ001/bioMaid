% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waldTest.R
\name{waldTest}
\alias{waldTest}
\alias{waldTest.asreml}
\title{Wald Tests for Fixed-Effect Contrasts Using Predicted Values}
\usage{
waldTest(
  pred,
  cc,
  by = NULL,
  test = c("Wald", "F"),
  df_error = NULL,
  adjust = c("none", "bonferroni", "holm", "fdr", "BH", "BY")
)

waldTest.asreml(
  object,
  classify,
  cc,
  by = NULL,
  test = c("Wald", "F"),
  adjust = c("none", "bonferroni", "holm", "fdr", "BH", "BY"),
  ...
)
}
\arguments{
\item{pred}{List returned by \code{predict(model, classify = ..., vcov = TRUE)}.
Must contain elements \code{pvals} (data frame of predictions) and \code{vcov}
(prediction error variance--covariance matrix).}

\item{cc}{Named list of test specifications.  Each element is itself
a list with fields:
\describe{
\item{\code{coef}}{Integer indices \strong{or} character labels identifying which
rows of \code{pred$pvals} are involved.  When \code{by} is used, indices/labels
refer to rows \strong{within the group}.  Character labels are matched
against the pasted factor-level combination (e.g. \code{"T0:G01"} for a
\code{Treatment:Genotype} prediction).}
\item{\code{type}}{\code{"con"} (contrast) or \code{"zero"} (joint zero test).}
\item{\code{comp}}{For \code{type = "con"} only. One of:
a numeric contrast vector (length = \code{length(coef)});
a numeric contrast matrix (rows = contrasts, cols = \code{length(coef)});
\code{"pairwise"} to generate all pairwise differences automatically;
\code{NULL} or \code{"helmert"} for the default last Helmert contrast
(a warning is issued).}
\item{\code{group}}{Optional named list \code{list(left = ..., right = ...)} to
override the auto-generated \code{"A vs B"} row labels.}
}}

\item{by}{Character string \strong{or character vector} naming one or more
factor columns in \code{pred$pvals} to split by.  When a single name is
supplied the rows are split by that column's levels.  When a vector of
names is supplied (e.g. \code{by = c("Site", "Year")}) the levels of those
columns are pasted together to form an interaction grouping variable, and
the output grouping column is named with the same pasted string (e.g.
\code{"Site:Year"}).  The same \code{cc} specification is applied independently
within each group, with \code{coef} referring to within-group row positions.
Default \code{NULL} (no splitting).}

\item{test}{\code{"Wald"} (default) for asymptotic \eqn{\chi^2} tests, or
\code{"F"} for \eqn{F}-tests.  \code{"F"} requires \code{df_error}.}

\item{df_error}{Numeric error degrees of freedom for F-tests (e.g.
\code{model$nedf}).  Ignored when \code{test = "Wald"}.}

\item{adjust}{P-value adjustment method passed to \code{\link[=p.adjust]{p.adjust()}}.  Applied
across all contrasts within each group.  Options:
\describe{
\item{\code{"none"}}{No adjustment (default).}
\item{\code{"bonferroni"}}{Bonferroni correction  -- controls family-wise error
rate (FWER) conservatively.}
\item{\code{"holm"}}{Holm step-down  -- controls FWER less conservatively than
Bonferroni.}
\item{\code{"fdr"} / \code{"BH"}}{Benjamini--Hochberg False Discovery Rate.
Controls the expected proportion of false discoveries among rejected
hypotheses.  Best choice when many contrasts are tested and some true
effects are expected.}
\item{\code{"BY"}}{Benjamini--Yekutieli FDR.  Valid under arbitrary (including
positive) dependence between test statistics; more conservative than
\code{"BH"} but provides stronger guarantees.}
}}

\item{object}{An ASReml-R V4 model object.}

\item{classify}{Character string passed to \code{asreml::predict.asreml()} as
the \code{classify} argument.}

\item{...}{Additional arguments forwarded to
\code{asreml::predict.asreml()}.}
}
\value{
A named list with elements:
\describe{
\item{\code{Contrasts}}{Data frame of contrast test results (or \code{NULL} if no
\code{"con"} elements were supplied).  Columns: grouping variable (if \code{by}
used), \code{Comparison}, \code{Estimate}, \code{Std.Error},
\code{Wald.Statistic} or \code{F.Statistic}, \code{df} (numerator), \code{P.Value}.}
\item{\code{Zero}}{Data frame of joint zero test results (or \code{NULL}).
Columns: grouping variable (if \code{by} used), \code{Test},
\code{Wald.Statistic} or \code{F.Statistic}, \code{df}, \code{P.Value}.}
\item{\code{test}}{The \code{test} argument used.}
\item{\code{adjust}}{The \code{adjust} argument used.}
}
}
\description{
Performs Wald tests on linear contrasts of predicted values obtained from
\code{asreml::predict.asreml()}.  Working directly with \code{predict()} output
means contrasts are specified through \strong{meaningful factor-level labels}
rather than raw coefficient indices, and the prediction error
variance--covariance matrix (\code{pred$vcov}) is used directly  -- no access to
model internals required.

Two test types are available for each element of \code{cc}:
\describe{
\item{\code{"con"}  -- contrasts}{Test \eqn{H_0: \bm{c}^\top\hat{\bm{\tau}} = 0}
for a specified contrast vector \eqn{\bm{c}}.  The Wald statistic is
\eqn{W = (\bm{c}^\top\hat{\bm{\tau}})^2 / \bm{c}^\top\text{Vcov}\,\bm{c}
    \sim \chi^2_1} (or \eqn{F_{1,\nu}} with \code{test = "F"}).  Set
\code{comp = "pairwise"} to automatically generate all \eqn{\binom{k}{2}}
pairwise contrasts for \eqn{k} specified levels.}
\item{\code{"zero"}  -- joint zero equality}{Test \eqn{H_0: \bm{Z}\hat{\bm{\tau}} = \bm{0}}
for a set of \eqn{q} coefficients simultaneously.  The statistic is
\eqn{W = \hat{\bm{\tau}}^\top\bm{Z}^\top(\bm{Z}\,\text{Vcov}\,\bm{Z}^\top)^{-1}
    \bm{Z}\hat{\bm{\tau}} \sim \chi^2_q} (or \eqn{F_{q,\nu}/q}).}
}
}
\section{Functions}{
\itemize{
\item \code{waldTest.asreml()}: Convenience method that calls \code{asreml::predict.asreml()}
internally.  \code{classify} is required; \code{test = "F"} uses \code{object$nedf}
automatically.

}}
\examples{
\dontrun{
## --- predict first, then test ---
pred <- predict(model, classify = "Treatment", vcov = TRUE)

## Pairwise contrasts among all three treatment levels
res <- waldTest(pred,
                cc = list(list(coef = c("N0","N1","N2"),
                               type = "con",
                               comp = "pairwise")))

## Manual contrast: N2 vs N0
res2 <- waldTest(pred,
                 cc = list(list(coef = c("N0","N2"),
                                type = "con",
                                comp = c(-1, 1),
                                group = list(left="N2", right="N0"))))

## Joint zero test: are N1 and N2 effects simultaneously zero?
res3 <- waldTest(pred,
                 cc = list(list(coef = c("N1","N2"),
                                type = "zero",
                                group = "N1:N2 joint")))

## F-tests with denominator df from model
res4 <- waldTest(pred, cc = list(...),
                 test = "F", df_error = model$nedf)

## Run within each site (by-group, single factor)
pred_site <- predict(model, classify = "Treatment:Site", vcov = TRUE)
res5 <- waldTest(pred_site,
                 cc   = list(list(coef = c("N0","N1","N2"),
                                  type = "con", comp = "pairwise")),
                 by   = "Site",
                 test = "F", df_error = model$nedf,
                 adjust = "fdr")

## Run within each Site x Year combination (multi-factor by)
pred_sy <- predict(model, classify = "Treatment:Site:Year", vcov = TRUE)
res5b <- waldTest(pred_sy,
                  cc  = list(list(coef = c("N0","N1","N2"),
                                  type = "con", comp = "pairwise")),
                  by  = c("Site", "Year"),
                  test = "F", df_error = model$nedf)

## Convenience: pass the model directly
res6 <- waldTest(model, classify = "Treatment",
                 cc = list(list(coef=c("N0","N2"), type="con", comp=c(-1,1))),
                 test = "F")
}

}
\seealso{
\code{asreml::predict.asreml()}
}
